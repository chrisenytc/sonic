'use strict';

module.exports = {
    enabled: true,
    origin: '*',
    credentials: true,
    methods: 'GET, POST, PUT, DELETE, OPTIONS, HEAD',
    headers: 'Origin, x-requested-with, content-type, accept'
};